# ─── Supabase (frontend) ─────────────────────────────────────────────────────
# Présentes dans le bundle client — c'est NORMAL pour la clé anon.
# La sécurité repose sur la RLS (voir scripts/security_hardening.sql), pas sur le secret.
VITE_SUPABASE_URL="https://votre-projet.supabase.co"
VITE_SUPABASE_ANON_KEY="votre_cle_anon_publique"

# ─── Scripts d'administration (Node, côté serveur UNIQUEMENT) ────────────────
# NE JAMAIS exposer côté client. Réservé aux scripts d'import/seed.
SUPABASE_SERVICE_ROLE_KEY="votre_cle_service_role_SECRETE"
