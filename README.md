# Antigravity

Ivoire Business Club web app built with React, TypeScript, Vite, Firebase, and Tailwind CSS.

## Run Locally

```bash
npm install
cp .env.example .env
npm run dev
```

Fill `.env` with the Firebase values before using authentication or Firestore features.

## Build

```bash
npm run build
```
